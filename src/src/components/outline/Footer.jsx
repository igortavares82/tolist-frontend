import React from 'react';
import { Layout, Row, Col } from 'antd';
const { Footer: AntFooter } = Layout;

const Footer = props => {

    return (
        <AntFooter className="outline-box outline-footer">
            footer
        </AntFooter>
    );
}

export default Footer;